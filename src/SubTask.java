public class SubTask extends Task {
    /**
     * Класс сабтаска наследуется от Task.
     *
     * @param name
     * @param detail
     * @param status
     */
    public SubTask(String name, String detail, Status status) {
        super(name, detail, status);
    }
}
